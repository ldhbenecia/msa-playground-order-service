server:
  port: 8082

spring:
  application:
    name: order-service
  profiles:
    active: dev

  config:
    import: "configserver:http://localhost:9000"

  cloud:
    config:
      username: admin
      password: admin
